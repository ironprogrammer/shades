SCHEDULE = {
    "enabled": True,
    "time": "07:30",
    "days": ["mon", "tue", "wed", "thu", "fri"],
}

# Map each shade to its target position (0 = open, 100 = closed).
# Different shades may have different positions for fine-tuning.
SHADES = {
    "Your Shade Name": 0,
}


def should_run(ctx):
    return True


async def run(hub):
    from hub import apply_shades
    await apply_shades(hub, SHADES)
